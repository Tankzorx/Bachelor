extends ../layouts/layout

block content
  h2 Third Party Profile(s)
  each available_comp in available
    a.small(href='/makereservation?comp=' + available_comp + "&startDate=" + startDate + "&finishDate=" + finishDate) #{available_comp}